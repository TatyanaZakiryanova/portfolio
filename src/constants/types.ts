export interface IIcons {
  iconId: string;
  alt: string;
  title: string;
}

export interface IProjects {
  title: string;
  previewSrc: string;
  description: string;
  technologies: string;
  details: string;
  githubLink: string;
  deployLink?: string;
}
